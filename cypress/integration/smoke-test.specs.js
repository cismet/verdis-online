describe('Smoke-Test', () => {
    it('shows the landing page', () => {
    });
    it('logs in from the landing page via typing the STAC', () => {
    });
    it('logs in from the landing page with an STAC url paramter', () => {
    });
    it('loads the kassenzeichen', () => {
    });
    it('shows kassenzeicheninfo', () => {
    });
    it('displays the kassenzeichen on the map', () => {
    });
    it('selects the area in the list when the area is selected in the map', () => {
    });
    it('selects the area in the map when the area is selected in the lis', () => {
    });
    it('shows the help modal', () => {
    });
    it('downloads the FEB document', () => {
    });
    it('logs out', () => {
    });
});
